does the existing proxy in nono support filtering?

---

## PR Comments

**@scp7:** Provide an example on how I can configure and utilize a proxy with nono to granularly filter HTTP traffic?
For example if I wanted to remove JS code or images from a web page, so my agent would not have access to those. 

Does the existing nono proxy support this today?